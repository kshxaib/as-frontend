import { QuestionReviewWorkspace } from "./question-review";

/**
 * QuestionReview — Phase 4 entry point.
 * The page now lives in `components/question-review/*`
 * ("The Reading Room" quality-control workspace).
 * All store actions and API contracts are preserved unchanged.
 */
export const QuestionReview = () => <QuestionReviewWorkspace />;

export default QuestionReview;
