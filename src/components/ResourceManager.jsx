import { ResourceLibrary } from "./resources";

/**
 * ResourceManager — Phase 2 entry point.
 * The page now lives in `components/resources/*` ("The Reading Room" redesign).
 * All store actions and API contracts are preserved unchanged.
 */
export const ResourceManager = () => <ResourceLibrary />;

export default ResourceManager;
