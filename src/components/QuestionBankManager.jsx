import { QuestionBankLibrary } from "./question-banks";

/**
 * QuestionBankManager — Phase 3 entry point.
 * The page now lives in `components/question-banks/*`
 * ("The Reading Room" examination archive redesign).
 * All store actions and API contracts are preserved unchanged.
 */
export const QuestionBankManager = () => <QuestionBankLibrary />;

export default QuestionBankManager;
