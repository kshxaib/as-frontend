import { SolutionsWorkspace } from "./solutions";

/**
 * SolutionViewer — Phase 5 entry point.
 * The page now lives in `components/solutions/*`
 * ("The Reading Room" answer manuscript workspace).
 * All store actions and API contracts are preserved unchanged.
 */
export const SolutionViewer = () => <SolutionsWorkspace />;

export default SolutionViewer;
