import { CommunityLibrary } from "./community";

/**
 * CommunityHub — Phase 8 entry point.
 * The page now lives in `components/community/*`
 * ("The Reading Room" academic commons).
 * All store actions and API contracts are preserved unchanged;
 * guest accessibility is handled in App.jsx exactly as before.
 */
export const CommunityHub = () => <CommunityLibrary />;

export default CommunityHub;
