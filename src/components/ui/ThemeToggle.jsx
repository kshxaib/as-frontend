import React from 'react';

// Light mode has been permanently removed from AcademicStack.
// ThemeToggle renders null to keep the UI purely dark mode.
export const ThemeToggle = () => {
  return null;
};
